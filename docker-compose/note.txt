//start node server
docker exec -d -it nodeServer node /node/index.js

//enter bash node 
docker exec -it nodeServer "/bin/sh"

//exec all containers
docker compose up -d

//stop all containers
docker compose stop